const Course = require('../model/Course');
const Utils = require('../util/Utils');
const CourseService = require('../service/CourseService');

exports.createCourse = (req, res, next) => {
  // TODO: call function to create course and handle the error if it is
  //       After, send a proper response
};

exports.getCourseList = (req, res, next) => {
  // TODO: call function to get course list and handle the error if it is
  //       After, send a proper response
};

exports.getCourse = (req, res, next) => {
  // TODO: call function to get course and handle the error if it is
  //       After, send a proper response
};

exports.updateCourse = (req, res, next) => {
  // TODO: call function to update course and handle the error if it is
  //       After, send a proper response
};

exports.deleteCourse = (req, res, next) => {
  // TODO: call function to delete course and handle the error if it is
  //       After, send a proper response
};
